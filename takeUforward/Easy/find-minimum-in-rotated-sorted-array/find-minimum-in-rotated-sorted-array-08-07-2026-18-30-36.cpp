            ans = min(ans, arr[low]);
            low = mid +1;
      int high = n-1;
      int ans = arr[0];

      while (low <= high){
        int mid = (low+high)/2;
        if(arr[low] <= arr[mid]){
      int low =0;
      int n = arr.size();
    int findMin(vector<int> &arr)  {
class Solution {
public: