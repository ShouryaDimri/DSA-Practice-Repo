            for (int i = 0; i < n; i++) {

            long long totalhour = 0;
            // find total hours needed at rate mid

            int mid = (low + high) / 2;

        while (low <= high) {

        int high = temp[n - 1];
        // maximum possible rate is the largest pile

        int low = 1;
        // minimum possible rate is 1